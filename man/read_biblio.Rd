% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-biblio.R
\name{read_biblio}
\alias{read_biblio}
\title{Read bibliometric data}
\usage{
read_biblio(path, format = "auto", id = NULL, actors = NULL, sep = ";", ...)
}
\arguments{
\item{path}{Character. Path to a file, a vector of file paths, or a
directory containing export files.}

\item{format}{Character. File format:
\describe{
\item{\code{"auto"}}{Default. Auto-detect from file content.}
\item{\code{"scopus"}}{Scopus CSV.}
\item{\code{"wos"}}{Web of Science plaintext.}
\item{\code{"wos_tab"}}{Web of Science tab-delimited.}
\item{\code{"bibtex"}}{BibTeX .bib file.}
\item{\code{"ris"}}{RIS file.}
\item{\code{"dimensions"}}{Dimensions CSV.}
\item{\code{"lens"}}{Lens.org CSV.}
\item{\code{"openalex_csv"}}{Flat OpenAlex CSV export (pipe-delimited fields).}
\item{\code{"generic"}}{Any CSV. Use \code{id} and \code{actors} to specify columns.}
}}

\item{id}{Character. Column name for document identifier. Only used
when \code{format = "generic"}. Default \code{NULL} (uses row numbers).}

\item{actors}{Character vector. Column names to split into list-columns.
Only used when \code{format = "generic"}.}

\item{sep}{Character. Delimiter for splitting actor columns. Default \code{";"}.}

\item{...}{Additional arguments passed to the format-specific reader.}
}
\value{
A data frame.
}
\description{
Universal reader that handles files, folders, format detection, and
generic CSV input. Accepts a single file, multiple files, or a directory.
}
\examples{
\dontrun{
# Auto-detect single file
data <- read_biblio("export.csv")

# Read entire folder (merges all files)
data <- read_biblio("scopus_exports/")

# Multiple files
data <- read_biblio(c("scopus1.csv", "scopus2.csv"))

# Explicit format
data <- read_biblio("file.csv", format = "scopus")

# Generic CSV with custom columns
data <- read_biblio("my_data.csv", format = "generic",
                    id = "doc_id",
                    actors = c("Authors", "Keywords"),
                    sep = ";")
}
}
