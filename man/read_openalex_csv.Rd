% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-openalex.R
\name{read_openalex_csv}
\alias{read_openalex_csv}
\title{Read a flat OpenAlex CSV export}
\usage{
read_openalex_csv(file, sep = "|")
}
\arguments{
\item{file}{Path to the CSV file.}

\item{sep}{Character. Delimiter for multi-value fields. Default \code{"|"}.}
}
\value{
A data frame in the standard bibnets format: \code{id}, \code{title},
\code{year}, \code{journal}, \code{doi}, \code{cited_by_count}, \code{abstract}, \code{type},
plus list-columns \code{authors}, \code{references}, \code{keywords}, \code{affiliations},
\code{countries}. \code{abstract} and \code{references} are always \code{NA} / empty
(not available in the flat export).
}
\description{
Reads the flat CSV format downloaded directly from the OpenAlex website
(\code{openalex.org/works} exports). Multi-value fields are pipe-delimited (\code{|}).
This is distinct from the nested tibble produced by \code{\link[openalexR:oa_fetch]{openalexR::oa_fetch()}},
which is handled by \code{\link[=read_openalex]{read_openalex()}}.
}
\examples{
f <- system.file("extdata", "openalex_works.csv", package = "bibnets")
data <- read_openalex_csv(f)
}
