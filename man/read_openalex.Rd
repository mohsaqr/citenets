% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-openalex.R
\name{read_openalex}
\alias{read_openalex}
\title{Convert OpenAlex data to citenets format}
\usage{
read_openalex(data)
}
\arguments{
\item{data}{A data frame from \code{oa_fetch(entity = "works", ...)}. Must
contain at least an \code{id} column. Common columns include \code{display_name},
\code{publication_year}, \code{so}, \code{doi}, \code{cited_by_count}, \code{referenced_works},
\code{ab}, and \code{author} (nested).}
}
\value{
A data frame with the same column structure as \code{\link[=read_scopus]{read_scopus()}}.
}
\description{
Takes the output of \code{\link[openalexR:oa_fetch]{openalexR::oa_fetch()}} (a tibble/data frame of works)
and converts it to the standardized citenets format with list-columns.
}
\examples{
\dontrun{
library(openalexR)
raw <- oa_fetch(entity = "works", search = "bibliometric networks",
                count_only = FALSE)
data <- read_openalex(raw)
}
}
