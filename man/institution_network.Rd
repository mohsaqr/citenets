% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/institution-network.R
\name{institution_network}
\alias{institution_network}
\title{Build an institution network}
\usage{
institution_network(
  data,
  type = "collaboration",
  counting = "full",
  similarity = "none",
  threshold = 0,
  min_occur = 1L,
  top_n = NULL
)
}
\arguments{
\item{data}{A data frame with \code{id} and \code{affiliations} (list-column of
institution names). For coupling, also needs \code{references}.}

\item{type}{Character. \code{"collaboration"} (default), \code{"coupling"}, or
\code{"equivalence"}.}

\item{counting}{Character. Counting method. Default \code{"full"}.}

\item{similarity}{Character. Similarity measure. Default \code{"none"}.}

\item{threshold}{Numeric. Minimum edge weight. Default 0.}

\item{min_occur}{Integer. Minimum papers per institution. Default 1.}

\item{top_n}{Integer or NULL. Return only the top n edges by weight.
Default NULL (all edges).}
}
\value{
A data frame with columns \code{from}, \code{to}, \code{weight}, \code{count}, \code{shared}.
}
\description{
Constructs a network between institutions (affiliations).
}
\examples{
\dontrun{
institution_network(data, "collaboration")
}
}
