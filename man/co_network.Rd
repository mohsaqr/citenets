% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/co-network.R
\name{co_network}
\alias{co_network}
\title{Build a co-occurrence network from any field}
\usage{
co_network(
  data,
  field,
  by = NULL,
  sep = ";",
  count = "full",
  measure = "none",
  threshold = 0,
  min_occur = 1L
)
}
\arguments{
\item{data}{A data frame with column \code{id} and the specified field(s).}

\item{field}{Character. The entity field — determines what the nodes are.}

\item{by}{Character or \code{NULL}. What links the nodes. If \code{NULL} (default),
entities are linked by co-occurring in the same document. If specified,
entities are linked when they share values from the \code{by} field.}

\item{sep}{Character or \code{NULL}. Delimiter for splitting character columns.
Default \code{";"}. Set to \code{NULL} if columns are already list-columns.}

\item{count}{Character. Counting method. Default \code{"full"}.}

\item{measure}{Character. Normalization method. Default \code{"none"}.}

\item{threshold}{Numeric. Minimum edge weight. Default 0.}

\item{min_occur}{Integer. Minimum entity frequency. Default 1.}
}
\value{
A data frame with columns \code{from}, \code{to}, \code{weight}, \code{count}, \code{shared}.
}
\description{
With one field, entities are linked when they co-occur in the same
document. With \code{by}, entities are linked when they share values of the
\code{by} field across documents.
}
\details{
Fields can be list-columns (already split) or character columns with
delimiters (auto-split via \code{sep}).
}
\examples{
data(biblio_data)

# Co-occurrence: keywords appearing in the same document
co_network(biblio_data, "keywords")

# Authors linked by shared keywords
co_network(biblio_data, "authors", by = "keywords")

# Keywords linked by shared authors
co_network(biblio_data, "keywords", by = "authors")

# Journals linked by shared references (= journal coupling)
co_network(biblio_data, "journal", by = "references", measure = "cosine")

# Auto-splits semicolon-delimited string columns
d <- data.frame(id = 1:3, tags = c("ml; dl; nlp", "ml; cv", "dl; cv"))
co_network(d, "tags")
}
